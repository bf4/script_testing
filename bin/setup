#!/usr/bin/env bash

set -eo pipefail

main() {
  if [ "$(uname -s)" = "Darwin" ]; then
    ./bin/setup-deps-osx
  else
    ./bin/setup-deps-linux
  fi
  rm -rf bats
  git clone --depth 1 https://github.com/sstephenson/bats.git
}

main
